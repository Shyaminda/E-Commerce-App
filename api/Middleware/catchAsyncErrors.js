// import express from 'express';

// const app=express();

// app.use((req,res,next)=>{
//     Promise.resolve(next(req,res,next)).catch(next);
// });



// module.exports = (theFunc) => (req, res, next) => {
//     Promise.resolve(theFunc(req, res, next)).catch(next);
//   };